# LeetCode Problem 557: Reverse Words in a String III (Easy)

## 🧩 Problem Statement

Given a string `s`, reverse the order of characters in each word while preserving:

- The original order of the words.
- The whitespace between words.

A word is defined as a sequence of non-space characters.

### Constraints

- `1 <= s.length <= 5 * 10^4`
- `s` contains printable ASCII characters.
- Words are separated by a single space.
- The solution should use:
  - **O(n) time complexity**
  - **O(1) extra space** (excluding the input string)

---

## Example 1

```text
Input:
s = "Let's take LeetCode contest"

Output:
"s'teL ekat edoCteeL tsetnoc"
